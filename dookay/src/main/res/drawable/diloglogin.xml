<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 圆角 -->
    <corners android:radius="@dimen/offset_6" />

    <padding
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp" />
    <!-- 各方向的间隔 -->
    <!-- 填充 -->
    <solid
        android:width="0.5dp"
        android:color="#000000" />
    <!-- 填充的颜色 -->
    <!--描边-->
    <stroke
        android:width="0.5dp"
        android:color="#000000" />
</shape>